# AI 모의면접 시스템 - 의존성 패키지
# 이 파일은 단순히 라이브러리 이름을 나열하는 것을 넘어, 협업과 배포 과정에서 시스템의 안정성을 보장하는 아주 중요한 역할을 합니다.
# 설치: pip install -r requirements_integrated.txt

# ========== 웹 프레임워크 ==========
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0

# ========== WebRTC ==========
aiortc>=1.6.0

# ========== LLM (Ollama) ==========
langchain>=0.1.0
langchain-ollama>=0.0.1
langchain-core>=0.1.0
langchain-community>=0.0.1
langchain-text-splitters>=0.0.1

# ========== 화이트보드 분석 (Claude Vision) ==========
anthropic>=0.18.0

# ========== TTS (Hume AI) ==========
hume>=0.5.0
aiohttp>=3.9.0
httpx>=0.25.0

# ========== STT (Deepgram — 클라우드, 우선) ==========
deepgram-sdk>=3.0.0
pyaudio>=0.2.14
pykospacing                    # 한국어 띄어쓰기 보정 (STT 후처리)

# ========== STT (Whisper — 오프라인 폴백) ==========
faster-whisper>=1.0.0          # CTranslate2 기반 고속 로컬 STT
# openai-whisper               # faster-whisper 대안 (선택)

# ========== RAG (PostgreSQL + PGVector) ==========
langchain-postgres>=0.0.1
psycopg2-binary>=2.9.9
pgvector>=0.2.0
pypdf>=3.17.0
sqlalchemy>=2.0.0

# ========== 감정 분석 ==========
deepface>=0.0.79
opencv-python>=4.8.0
numpy>=1.24.0
tf-keras>=2.15.0

# ========== 비동기 작업 (Celery) ==========
celery>=5.3.0
redis>=5.0.0
kombu>=5.3.0
flower>=2.0.0                  # 모니터링 UI (선택사항)

# ========== 인증 / 보안 ==========
python-jose[cryptography]>=3.3.0   # JWT 토큰 인증
bcrypt>=4.0.0                      # 비밀번호 해싱
cryptography>=41.0.0               # SSL 인증서 생성

# ========== PDF 리포트 ==========
reportlab>=4.0.0               # PDF 면접 리포트 생성 (한국어 폰트 지원)

# ========== 유틸리티 ==========
python-dotenv>=1.0.0
requests>=2.31.0
websockets>=12.0

# ========== 선택사항 (Optional) ==========
# langgraph>=0.0.1             # LangGraph 워크플로우
# tqdm>=4.66.0                 # 진행률 표시
# openai-whisper               # faster-whisper 대안 (로컬 STT)
